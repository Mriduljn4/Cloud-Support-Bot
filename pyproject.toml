[project]
name = "cloud-support-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mriduljn4", email = "mriduljn4@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
cloud-support-bot = "cloud_support_bot:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
